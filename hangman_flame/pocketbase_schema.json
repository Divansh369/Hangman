{
  "meta": {
    "generatedFrom": "inferred from code (lib/**)",
    "note": "Verify these fields with your PocketBase admin. Some PocketBase-specific options (field IDs, visibility, indexes) are not included and should be configured in the PB admin UI or via export/import."
  },
  "collections": [
    {
      "name": "users",
      "description": "Application users (PocketBase auth collection)",
      "fields": [
        { "name": "username", "type": "text", "required": true },
        { "name": "email", "type": "email", "required": true },
        { "name": "password", "type": "password", "required": true },
        { "name": "score", "type": "number", "required": false, "default": 0 }
      ]
    },
    {
      "name": "rooms",
      "description": "Multiplayer room state",
      "fields": [
        { "name": "code", "type": "text", "required": true },
        { "name": "host", "type": "relation", "target": "users", "required": true },
        { "name": "opponent", "type": "relation", "target": "users", "required": false },
        { "name": "status", "type": "text", "required": true, "allowedValues": ["waiting", "playing", "ended"] },
        { "name": "guessedLetters", "type": "json", "description": "array of letters guessed", "required": false },
        { "name": "wrongGuesses", "type": "number", "required": false },
        { "name": "turn", "type": "relation", "target": "users", "required": false, "description": "id of user whose turn it is" },
        { "name": "secretWord", "type": "text", "required": false, "private": true },
        { "name": "round", "type": "number", "required": false }
      ]
    },
    {
      "name": "messages",
      "description": "Chat messages linked to rooms",
      "fields": [
        { "name": "room", "type": "relation", "target": "rooms", "required": true },
        { "name": "user", "type": "relation", "target": "users", "required": true },
        { "name": "text", "type": "text", "required": true }
      ]
    },
    {
      "name": "user_progress",
      "description": "Per-user progress and collectibles",
      "fields": [
        { "name": "user", "type": "relation", "target": "users", "required": true },
        { "name": "levelsCompleted", "type": "json", "description": "array of level ids", "required": false },
        { "name": "collectibles", "type": "json", "description": "array of collectible ids", "required": false },
        { "name": "currency", "type": "number", "required": false, "default": 0 }
      ]
    }
  ]
}
